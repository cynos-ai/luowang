# 信号控制塔控制台视觉改版 Plan

- 目标版本：v0.6.1，负责人已确认。状态：设计/原型已完成；下述正式产品改版均未实施。
- 依赖：[spec](spec.md)。与 [多项目计划](../luowang-multi-project/plan.md) 的 UI 阶段同步实施；多项目行为仍由其 Spec/Plan 管辖。本需求只负责视觉和信息层级，不能把原型验收算作生产验收。

## 阶段 0：设计定稿（已完成，不代表产品落地）

- [x] 比较六种信息架构，确定信号控制塔；比较六种配色，确定海图蓝。
- [x] 在独立本地 HTML 演示中加入图谱网格、刻度与节点，覆盖总览、当前测试、Runs、场景、Git、配置和空闲/运行演示状态。
- [x] 记录合成数据和真实产品边界；原型仅静态资源与浏览器内状态，不访问罗网 API。

原型目录 `prototypes/` 不是产品构建入口，不能作为版本已实现的证据。

## 阶段 1：设计系统与壳（未开始）

- [ ] 在 `src/web/styles.css` 建立海图蓝语义 token、网格容器、实底面板、状态色和键盘焦点，不把原型 CSS 直接覆盖生产样式。
- [ ] 调整 `src/web/components/Shell.tsx`、登录与全局导航，保持真实健康信息和认证状态；页面结构允许未来项目切换、全局执行位置和账号入口。
- [ ] 检查大字、次要文字、按钮、输入框、状态徽标在深色/网格底及面板上的对比度和 200% 缩放。

证明：组件截图和键盘路径；不把演示 `GATEWAY ONLINE` 写死为真实状态。

## 阶段 2：真实六页迁移（未开始）

- [ ] 按总览 → 当前测试 → Runs → 场景 → Git → 配置顺序改 `src/web/App.tsx` 和 `src/web/settings/**` 的呈现层，复用真实 API 和已有 owner；总览改为空闲优先，执行步骤只在详情。
- [ ] 联合多项目 UI 阶段时加入项目列表、projectId 路由和部署/项目/账号设置；等待其真实 API/类型就绪，不在视觉分支造假接口、暗中选择项目或写固定状态。
- [ ] 覆盖未配置/加载/失败/陈旧/空队列/运行中/等待归档/归档失败/被阻塞及长文本；所有写操作仍走既有确认、认证和项目归属校验。

证明：通过类型检查、组件与浏览器 E2E，六页展示由真数据驱动；API 请求和写副作用与多项目 Spec 一致。

## 阶段 3：回归与 v0.6.1 发布验收（未开始）

- [ ] 桌面与窄屏对照设计，验证无整页横向溢出、键盘导航、对比度、错误/陈旧信息与长文阅读。
- [ ] 运行 format、lint、typecheck、unit、build、现有 UI/E2E；同版还须通过多项目隔离、迁移和双项目验收，视觉回归不能取代安全验收。
- [ ] 将 AC-ST-01～05 与多项目 AC-MP 同列为 v0.6.1 发布前验收项；发布说明明确视觉改版、真实数据边界和未覆盖限制，未通过时不得宣称正式改版完成。

风险：深色样式掩盖真实错误状态；首页把执行中、已完成和待归档混为一谈；旧布局与多项目项目切换二次冲突。采用分层 token、状态矩阵和先 API 后页面的顺序降低风险；回退视觉修改不能删除任何历史数据。
