# 非生产测试项目按 Run 自动清理

官网是专为罗网验证创建的非生产项目，负责人已允许直接处理测试数据。当前默认入口没有清理适配器；仅有自助账号删除会依赖模型持有的临时凭据，不能稳定覆盖运行结束后的收尾。

期望：测试结束后自动删除本 Run 标记的数据，独立确认没有残留，不影响其他 Run，也不以清理告警改写功能结果。沿用现有三组 Agent、四个正常 Session 和 Harness 收尾 owner。Token 不交给模型，不开放任意 URL/SQL/删除命令。非目标：生产数据、部署发布、全库重置或跨进程恢复系统。
