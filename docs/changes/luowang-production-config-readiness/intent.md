# 生产配置就绪

期望验证过的阶段思考策略成为产品行为，而非依赖评测驱动；新部署默认配置与验收一致，已有配置及历史记录不被改写。清理必须区分受控资源域，不能以官网账号查询为空冒充所有登记资源已清理。

保留三组 Agent 配置、正常四个隔离 Session、Secret Store 和既有工件职责。只覆盖非生产官网沙箱账号及关联会话；未知资源保留人工处理告警。禁止通用删除、任意路径、语义正则判定及生产数据操作。

负责人已批准显式清理资源域绑定。工程 PR 后继续准备正常、缺陷各一次真实验收，总模型请求上限300，不重复采样；远程写入必须在已授权信封内。发布和 tag 另行批准。

首轮因环境能力缺失而 blocked，负责人已追加授权：先补足零模型环境就绪证明，与 PR #66 一并合入；合并后独立授权新一轮300请求、正常/缺陷各一次，全绿预检才放行。不覆盖首轮，不用更多样本追分。
