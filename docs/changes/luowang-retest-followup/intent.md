# 复测问题收敛 Intent

Qwen复测暴露：Main发现错误后误以为计划只能写一次；Runner破坏已返回的证据链接；隔离MCP从证据目录启动npx时不能使用应用已安装依赖。

随后真实Qwen＋MCP联合验收发现：Reviewer缺少读取原始浏览器记录的通道，将快照/日志当成命令读取后，普通工具路由错误被永久归为证据失败；审核因此使用初始截图推断反向操作。需要让现有Reviewer读取必要原始事实并准确表达图像覆盖范围，不引入逐步截图证明矩阵。

原生DeepSeek联合复验又暴露报告范围与主体失真：一条已执行场景仍套用零场景通过说明，模型看图被称为人工确认，截图覆盖被泛称完整。期望报告只表达适用结论和证据能支持的范围，不靠扩大测试或禁止词门禁掩盖问题。

正常/已知注销缺陷的成对 DeepSeek 验收进一步发现：执行与审核已确认真实缺陷，但最终 Main 将查询 unavailable 写入仅允许 create/link 的 issue_action；格式反馈丢失具体字段，随后输出触顶而未交付报告。期望已确认 Bug 不因去重缺口丢失，格式错误可在原 Session 安全纠正；本次只修最终汇总并复用固定前置工件复验，不重跑 API 或扩大预算。

期望：保持单份权威工件但允许所属Session纠错更新，忠实引用证据，在断网镜像中使用已安装MCP。保留现有角色、Session数量与安全边界，不改历史评估、不操作共享服务或发布。早期定向模型测试使用Qwen；用户随后确认基础测试使用Coding Plan，最终候选测试使用原生DeepSeek。
